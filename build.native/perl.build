#!/bin/sh

wd=$(pwd)
cd $dir
rm -rf /tmp/stage
./configure.gnu
make -j$(nproc)
make DESTDIR=/tmp/stage install
rm /tmp/stage
cd $wd
yspm build --root /tmp/stage --output "$wd/perl.yspkg" --name "perl" --version "5.44.0" --abi yspm-abi-1 --arch x86_64 --depends "m4,zlib"
