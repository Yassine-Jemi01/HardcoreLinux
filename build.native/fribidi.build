

#!/bin/sh

wd=$(pwd)
cd $dir
rm -rf /tmp/stage
rm -rf build
mkdir build && cd build
meson setup .. --prefix=/usr -Ddocs=false --reconfigure
ninja
meson install --destdir=/tmp/stage
find /tmp/stage -name *.so -exec strip --strip-all {} \;
rm -rf /tmp/stage/usr/share/man
rm -rf /tmp/stage/usr/share/info
rm -rf /tmp/stage/usr/share/doc
cd $wd
yspm build --root /tmp/stage --output "$wd/fribidi.yspkg" --name "fribidi" --version "1.0.16" --abi yspm-abi-1 --arch x86_64
